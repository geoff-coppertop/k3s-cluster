---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: homer
  namespace: apps
spec:
  storageClassName: nfs-client
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi
