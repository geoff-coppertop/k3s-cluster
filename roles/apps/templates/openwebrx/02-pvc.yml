---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: openwebrx
  namespace: apps
spec:
  storageClassName: nfs-client
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi
